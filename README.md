# quran_shazam

Local repository for the Quran Shazam project (backend + mobile client).

This repo contains an API (FastAPI) and a Flutter mobile app. Large prebuilt FAISS indexes are excluded from the repository via `.gitignore`.

See `app/README.md` for development instructions for the backend.
